[project]
name = "lerobot-robot-meca500"
version = "0.0.1"
dependencies = [
    "lerobot",
    "mecademicpy",
    "bota_driver"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"